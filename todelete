function calc(){
  if(lepizza.size=="large"){
    var price=1000; 
    if(pizzaToppings=="Black olives"){
      price + 150

    }
    else if(pizzaToppings == "Bacon"){
      price + 100
    }
    else{
    price + 50
  }
  function crust(){
    if(pizzaCrust=="crispy"){
      price + 200
    }
    else if ( pizzaCrust == "stuffed"){
      price + 100
    }

    }
    
    
var totalPrice = price * PizzaQty

console.log(totalPrice)

  }
}